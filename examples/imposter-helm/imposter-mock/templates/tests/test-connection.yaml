apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "imposter-mock.fullname" . }}-test-connection"
  labels:
    {{- include "imposter-mock.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "imposter-mock.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
