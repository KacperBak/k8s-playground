apiVersion: v1
kind: Service
metadata:
  name: {{ include "imposter-mock.fullname" . }}
  labels:
    {{- include "imposter-mock.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "imposter-mock.selectorLabels" . | nindent 4 }}
